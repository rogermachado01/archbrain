---
type: React Component
title: Link
description: `Link` is a shared React component in the marketing web app's shared UI layer, providing consistent link rendering across the site. It appears on both the homepage (`/`) and dynamic slug-based pages (`/[slug]`), making it a common building block wherever pages need to render navigational links to other parts of the site.
level: component
icon: fe-component.svg
owner: contentful/team-workflows
ddd_subdomain: supporting
ddd_context: Layout & Navigation
ddd_role: Presentational Component
---

Because it's used across the root route and all dynamic slug routes, `Link` serves as the primary mechanism for in-app navigation throughout the marketing site's page templates, rather than being tied to a single page type or content section.
