---
type: React Component
title: Page Link
description: PageLink is the React component used for in-page navigation on both the home route (`/`) and dynamic content routes (`/[slug]`), rendering wherever the page needs a link to another route within the marketing site.
level: component
icon: fe-component.svg
owner: contentful/team-workflows
ddd_subdomain: supporting
ddd_context: Link Components
ddd_role: Link Component
---

It builds on the shared `Link` component from `@src/components/shared/link`, drawing on its `LinkProps` type to stay consistent with the app's broader link-handling conventions across templates.

# Relations

- [Link](link.md) — Wraps the shared Link component for consistent navigation {kind: sync}
